# Verification — 2026-09-25

## Проверено

- Desktop в Codex browser: главная, форма, Explore, граф знаний.
- Mobile viewport 390×844: главная, форма, реальный расчёт и вертикальный Explore.
- Выбор элемента и режима контроля обновляет активный узел и сведения.
- Формой рассчитана тестовая карта; показаны 4 столпа, скрытые стволы, проценты, Да Юнь и методика.
- Вход через реальную форму кабинета; сохранённая демо-карта открывается из списка клиентов.
- Учебная консультация сохранена через UI и отображена в истории.
- PDF: 2 страницы A4, Poppler rendering, кириллица и CJK без отсутствующих glyphs, без обрезанного текста на образце.
- TypeScript, production build, 18 engine tests, 10 API/DB integration checks.
- npm audit: 0 известных уязвимостей после override транзитивного esbuild для Drizzle.

## Не измерено / ограничения

- Реальный смартфон, GPU FPS, Lighthouse, screen reader не проверялись. Нельзя называть их проверки пройденными.
- Reduced motion реализован CSS; браузерная эмуляция этого media query отдельно не запускалась.
- SVG fallback — основной renderer. Материальные WebGL-эффекты и cinematic morph ещё не реализованы.
- Демонстрационные данные явно обозначены. Сохранённая учебная консультация не относится к реальному человеку.

Локальные снимки находятся в `artifacts/` (исключены из Git). Реальные секреты — только `.env`.
