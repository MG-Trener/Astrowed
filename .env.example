DATABASE_URL=
DATABASE_URL_UNPOOLED=
# Set these before enabling the private consultant workspace.
WORKSPACE_PASSWORD=
SESSION_SECRET=
# Explicit acknowledgement for migration into a development database.
ALLOW_DB_MIGRATION=false
# Optional Photon-compatible structured geocoder (public endpoint, no secret key).
NEXT_PUBLIC_COMPASS_GEOCODER_URL=https://photon.komoot.io/structured
