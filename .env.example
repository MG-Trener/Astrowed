DATABASE_URL=
DATABASE_URL_UNPOOLED=
# Set these before enabling the private consultant workspace.
WORKSPACE_PASSWORD=
SESSION_SECRET=
# Explicit acknowledgement for migration into a development database.
ALLOW_DB_MIGRATION=false
# Optional OSM Overpass endpoint for Russian address search (public URL, no secret key).
NEXT_PUBLIC_COMPASS_OSM_URL=https://maps.mail.ru/osm/tools/overpass/api/interpreter
# Client accounts: public service URLs (not credentials).
NEXT_PUBLIC_NEON_AUTH_URL=
NEXT_PUBLIC_ACCOUNT_API_URL=
# Backend only. Neon injects its auth URL in deployed Functions.
NEON_AUTH_BASE_URL=
ACCOUNT_ALLOWED_ORIGINS=https://astrowed.amratlas.kz
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
